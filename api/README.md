# Glastocam API

A Rust backend API built with Actix-web.

## Prerequisites

- Rust (https://rustup.rs/)

## Running the server

```bash
cd api
cargo run
```

The server will start at http://127.0.0.1:8080

## Endpoints

- `GET /` - Returns "Hello, World!"

